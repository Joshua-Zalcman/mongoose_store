<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mongoose Store</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css"
    />
    <style>
      article {
        max-width: 360px;
      }
      .card {
        margin: 0 auto;
      }
    </style>
  </head>
  <body>
    <div class="container valign center blue">
      <div class="row">
        <div class="col s12">
          <article class="card cyan">
            <div class="card-image">
              <img src="<%=product.img%>" alt="<%=product.name%>" />
              <%if(product.qty>0){%>

              <form
                action="/products/<%=product._id%>?_method=PUT"
                method="POST"
              >
                <input
                  type="text"
                  name="name"
                  value="<%=product.name%>"
                  hidden="true"
                />
                <input
                  type="text"
                  name="description"
                  value="<%=product.description%>"
                  hidden="true"
                />
                <input
                  type="text"
                  name="img"
                  value="<%=product.img%>"
                  hidden="true"
                />
                <input
                  type="number"
                  name="price"
                  value="<%=product.price%>"
                  hidden="true"
                /><input
                  type="number"
                  name="qty"
                  value="<%=product.qty-1%>"
                  hidden="true"
                />
                <input
                  type="submit"
                  value="Buy"
                  class="btn-floating halfway-fab btn-large"
                />
              </form>

              <%}else{%>
              <div class="col s4"><p>Out Of Stock</p></div>

              <%}%>
            </div>
            <h1 class="card-title"><%=product.name%></h1>
            <div class="card-content">
              <p><%=product.description%></p>
              <p>Price: $<%=product.price%></p>
              <p>Quantity: <%=product.qty%></p>
            </div>
            <div class="container">
              <div class="row">
                <div class="col s4">
                  <form
                    action="/products/<%=product._id%>?_method=DELETE"
                    method="POST"
                  >
                    <input
                      type="submit"
                      value="Delete"
                      class="btn red btn-block"
                    />
                  </form>
                </div>
                <div class="col s8">
                  <a
                    href="/products/<%=product._id%>/edit"
                    class="btn btn-block yellow"
                    >Edit</a
                  >
                </div>
              </div>
            </div>
            <br />
          </article>
          <br />
        </div>
      </div>
    </div>
    <a href="/products" class="btn">Go Back</a>
  </body>
</html>
